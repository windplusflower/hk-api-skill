# Evolution Notes

This directory stores source-backed learning notes that have already been applied to the formal rules.

- Each note preserves the original question, extracted facts, exact sources, and dedup marker.
- Use these files as an audit trail for low-risk knowledge that is already reflected in `rules/**`.
- Do not treat this directory as pending work.
